package self_Practice;

public class CalculatorTips {
    public static void main(String[] args) {
        /*String isSplit;
int numPeople;
double checkAmount;
String servQuality;

boolean splitCheck;
int tipPercent;

double totalPay;
double totalTip;
double totalPerPerson;
double tipPerPerson;


Scanner input = new Scanner(System.in);

sout("Split or no split (Yes or no)?");
isSplit = input.nextLine();

isSplit = isSplit.lower();

if (isSplit == "yes") {
    splitCheck = true;
} else if (isSplit == "no") {
    splitCheck = false;
} else {
    sout("Neither yes, nor no selected. Assuming no");
    splitCheck = false;
}

sout("Enter the number of people: ");

numPeople = input.nextInt();

sout("Enter the check amount: ");
checkAmount = input.nextDouble();

sout("How was the service quality?");
servQuality = input.nextLine();

servQuality = servQuality.lower();
if (servQuality == "poor") {
    tipPercent = 5;
} else if (servQuality == "fair") {
    tipPercent = 10;
} else if (servQuality == "good") {
    tipPercent = 15;
} else if (servQuality == "great") {
    tipPercent = 20;
} else if (servQuality == "excellent") {
    tipPercent = 25;
} else {
    tipPercent = 0;
}


totalTip = checkAmount * tipPercent / 100.0;

totalPay = checkAmount + totalTip;

if (splitCheck == true) {
    totalPerPerson = totalPay / numPeople;
    tipPerPerson = totalTip / numPeople;
} else {
    totalPerPerson = totalPay;
    tipPerPerson = totalTip;
}

sout("Number of people: " + numPeople);
sout("Total to pay: " + totalPay);
sout("Total tip: " + totalTip);
sout("Total Per person: " + totalPerPerson);
sout("Tip Per person: " + tipPerPerson);

*/
    }
}
